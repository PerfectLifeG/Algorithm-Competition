#include<bits/stdc++.h>
using namespace std;
const int N=100010;
int c[N],a[N],b[N];
int main(){
    ios::sync_with_stdio(0);cin.tie(0);
    int n,m;cin>>n>>m;
    for(int i=0;i<n;i++){
        cin>>c[i];
    }
    return 0;
}